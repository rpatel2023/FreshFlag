/// FreshFlag constants that are safe to ship with the client.
library;

const String appName = 'FreshFlag';
const String appVersion = '1.0.0';

/// Temporary Phase 1 local reminder defaults.
/// Household-configurable server-side rules replace these in later phases.
const int defaultExpiryReminderDays = 3;
const int urgentExpiryReminderDays = 1;
